#ifndef __MY_UTIL_H__
#define __MY_UTIL_H__

//#define MAX(a,b) ((a > b) ? (a) : (b))
//#define ABS(a) ((a > 0) ? (a) : -(a))
extern short myMax(short a, short b);
extern short myAbs(short a);

#endif
